import './styleLogin.css'

export default function Login (props) {
    return (
        <div className = {props.color}> Welcome to
        <h1 id= 'fonts'> NOTES</h1>
        <button> Sign in with Google </button>
         </div>
    )
    
};